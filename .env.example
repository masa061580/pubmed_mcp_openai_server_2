# NCBI E-utilities API Configuration (all optional)
# NCBI_API_KEY=your_ncbi_api_key_here  # Optional: get higher rate limits (10 req/s vs 3 req/s)
# NCBI_TOOL_NAME=PubMedMCPServer       # Optional: custom tool name
# NCBI_EMAIL=your.email@example.com    # Optional: contact email (uses default if not provided)

# Server Configuration
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=INFO